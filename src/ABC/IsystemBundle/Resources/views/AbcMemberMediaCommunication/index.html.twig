{% extends '::base.html.twig' %}

{% block body -%}
    <h1>AbcMemberMediaCommunication list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Media</th>
                <th>Createddate</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('isystem_admin_mediacommunication_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.media }}</td>
                <td>{% if entity.createdDate %}{{ entity.createdDate|date('Y-m-d H:i:s') }}{% endif %}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('isystem_admin_mediacommunication_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('isystem_admin_mediacommunication_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('isystem_admin_mediacommunication_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
