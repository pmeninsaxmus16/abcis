{% extends "ABCIsystemBundle::layoutAdmin.html.twig" %}
{% block title %} New Member {% endblock %}
{% block scripthead %}
 <script src="{{ asset('Resources/js/bootstrap-dropdown.js') }}"></script>
{% endblock %}
{% block bodycontenido -%}
    <h3{{ block('title')}}</h3>

    <form action="{{ path('isystem_admin_members_create') }}" method="post" {{ form_enctype(form) }}>
        {{ form_widget(form) }}
        <p>
            <button type="submit">Create</button>
        </p>
    </form>

        <ul class="record_actions">
    <li>
        <a href="{{ path('isystem_admin_members') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}
