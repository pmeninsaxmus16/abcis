{% extends "ABCIsystemBundle::layoutAdmin.html.twig" %}
{% block title %} Grade {% endblock %}
{% block scripthead %}
 <script src="{{ asset('Resources/js/bootstrap-dropdown.js') }}"></script>
{% endblock %}


{% block bodycontenido %}
{#
<h3>{{block('title')}}</h3>
#}
    <table class="table table-bordered">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Grade</th>
                <td>{{ entity.name }}</td>
            </tr>
            <tr>
                <th>Academic Order</th>
                <td>{{ entity.academicOrder }}</td>
            </tr>
        </tbody>
    </table>
    <p>
        <button class="btn" data-dismiss="modal" aria-hidden="true"><i class="icon-list"></i> Back to the list</button>
    </p>
    {#
    <ul class="record_actions">
    <li>
        <a href="{{ path('isystem_admin_mtto_grade') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('isystem_admin_mtto_grade_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>
        <form action="{{ path('isystem_admin_mtto_grade_delete', { 'id': entity.id }) }}" method="post">
            <input type="hidden" name="_method" value="DELETE" />
            {{ form_widget(delete_form) }}
            <button type="submit">Delete</button>
        </form>
    </li>
    </ul>
    #}
{% endblock %}
