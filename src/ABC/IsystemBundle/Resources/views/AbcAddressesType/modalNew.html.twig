    <div id="addnew" class="modal hide fade in"  style="display: none;">  
    <div class="modal-header">
    <button type="button" class="close" data-dismiss="modal" aria-hidden="true">×</button>
    <h3 id="myModalLabel">New Addresses Types</h3>
  </div>
  <div class="modal-body">
    <form action="{{ path('isystem_admin_mtto_addressestype_create') }}" method="post" {{ form_enctype(form) }}>
        {{ form_widget(form) }}
        {#<p>
            <button type="submit" class="btn">Create</button>
             <a href="{{ path('isystem_admin_mtto') }}" class="btn">
            Back to the list
        </a>
        </p>#}
    </form>
  </div>
  <div class="modal-footer"> 
    <button class="btn" data-dismiss="modal" aria-hidden="true"><i class="icon-list"></i> Back to the list</button>
    <button class="btn btn-primary">Save</button>
  </div>
</div>
