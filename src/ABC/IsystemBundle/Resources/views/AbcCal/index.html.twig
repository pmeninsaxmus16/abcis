{% extends "ABCIsystemBundle::layoutAdmin.html.twig" %}
{% block title %} Cal List {% endblock %}

{% block scripthead %}
 <script src="{{ asset('Resources/js/bootstrap-tooltip.js') }}"></script>
{% endblock %}
 
{% block sidebar %}{% endblock %} 

{% block contenido %}
{% import "ABCIsystemBundle:Default:tools.html.twig" as print %}
<div class="span12 ">
<h3>{{block('title')}}</h3>  
<p>
<a href="#save" class="btn btn-primary"  data-toggle="modal">
<i class="icon-plus icon-white"></i> Add new</a>
</p>
<table class="table table-bordered table-hover table-condensed">
        <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>Created Date</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><!--<a href="{{ path('isystem_admin_mtto_cal_show', { 'id': entity.id }) }}">-->{{ entity.id }}<!--</a>--></td>
                <td>{{ entity.name }}</td>
                <td>{% if entity.createdDate %}{{ entity.createdDate|date('H:i:s d-m-Y ') }}{% endif %}</td>
                <td>
                     {{ print.action('isystem_admin_mtto_cal_show','isystem_admin_mtto_cal_edit', entity.id)}}
                {#
                <ul>
                    <li>
                        <a href="{{ path('isystem_admin_mtto_cal_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('isystem_admin_mtto_cal_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                #}
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
{#
        <ul>
        <li>
            <a href="{{ path('isystem_admin_mtto_cal_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
#}
{{ print.modalaction('Cals','save')}}
{{ print.modalaction('Cals','edit')}}
{{ print.modalaction('Cals','show')}}
</div>
{% endblock %}
{% block javascripts %}
<script>
$(document).ready(function(){
   $('#save').on('show', function () {
         $('.modal-body-save').load('{{ path('isystem_admin_mtto_cal_new') }}');
  // do something…
   });
   $('#edit').on('show', function () {
        
  // do something…
    });
   $('#show').on('show', function () {
         
  // do something…
    });
});


</script>
{% endblock %}
