{% extends "ABCIsystemBundle::layoutAdmin.html.twig" %}
{% block title %} New Cal {% endblock %}
{% block scripthead %}
 <script src="{{ asset('Resources/js/bootstrap-dropdown.js') }}"></script>
{% endblock %}
{% block bodycontenido -%}

    {#<h3>{{block('title')}}</h3>#}
    

    <form action="{{ path('isystem_admin_mtto_cal_create') }}" method="post" {{ form_enctype(form) }}>
        {{ form_widget(form) }}
        <p>
        {#
        <a href="{{ path('isystem_admin_mtto_cal') }}" class="btn"><i class="icon-list"></i>
            Back to the list
        </a>
        #}
        <button class="btn" data-dismiss="modal" aria-hidden="true"><i class="icon-list"></i> Back to the list</button>                
        <button type="submit"class="btn btn-primary"><i class=""></i> Save</button>
        </p>
    </form>
{#
        <ul class="record_actions">
    <li>
        <a href="{{ path('isystem_admin_mtto_cal') }}">
            Back to the list
        </a>
    </li>
</ul>
#}
{% endblock %}
