{% extends '::base.html.twig' %}

{% block body -%}
    <h1>AbcPhoto list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Photo</th>
                <th>Weight</th>
                <th>Type</th>
                <th>Size</th>
                <th>Ip</th>
                <th>Photoname</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('isystem_admin_photo_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.photo }}</td>
                <td>{{ entity.weight }}</td>
                <td>{{ entity.type }}</td>
                <td>{{ entity.size }}</td>
                <td>{{ entity.ip }}</td>
                <td>{{ entity.photoname }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('isystem_admin_photo_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('isystem_admin_photo_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('isystem_admin_photo_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
