{% extends "ABCIsystemBundle::layoutAdmin.html.twig" %}
{% block title %} New School Year {% endblock %}
{% block scripthead %}
 <script src="{{ asset('Resources/js/bootstrap-dropdown.js') }}"></script>
{% endblock %}
{% block bodycontenido -%}

    <form action="{{ path('isystem_admin_mtto_sy_create') }}" method="post" {{ form_enctype(form) }}>
        {{ form_widget(form) }}
        <p>
        <button class="btn" data-dismiss="modal" aria-hidden="true"><i class="icon-list"></i> Back to the list</button>                
        <button type="submit"class="btn btn-primary"><i class=""></i> Save</button>
        </p>
    </form>
        
    {#
    <ul class="record_actions">
    <li>
        <a href="{{ path('isystem_admin_mtto_sy') }}">
            Back to the list
        </a>
    </li>
    </ul>
    #}
{% endblock %}
{% block javascriptsOthers %}
<script>
$(document).ready(function(){  
     $("select").each(function () {
     $(this).addClass('span2');
    });
 });
</script>
{% endblock %}