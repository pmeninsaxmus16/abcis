{% extends "BookingBundle::layoutMatto.html.twig" %}
{% block title %} City Show {% endblock %}
{% block scripthead %}
 <script src="{{ asset('Resources/js/bootstrap-dropdown.js') }}"></script>
{% endblock %}


{% block bodycontenido %}


    <table class="table table-bordered">

        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Quantity</th>
                <td>{{ entity.quantity }}</td>
            </tr>
            <tr>
                <th>Start time</th>
                <td>{{ entity.startTime|date('H:i:s') }}</td>
            </tr>
            <tr>
                <th>End time</th>
                <td>{{ entity.endTime|date('H:i:s') }}</td>
            </tr>
          {#  <tr>
                <th>Created date</th>
                <td>{{ entity.bdate|date('Y-m-d H:i:s') }}</td>
            </tr>   #}
        </tbody>
    </table>
    <p>
    <button class="btn" data-dismiss="modal" aria-hidden="true"><i class="icon-list"></i> Back to the list</button>
    </p>
{#
        <ul class="record_actions">
    <li>
        <a href="{{ path('booking_mtto_bkbookingevents') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('booking_mtto_bkbookingevents_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>
        <form action="{{ path('booking_mtto_bkbookingevents_delete', { 'id': entity.id }) }}" method="post">
            <input type="hidden" name="_method" value="DELETE" />
            {{ form_widget(delete_form) }}
            <button type="submit">Delete</button>
        </form>
    </li>
</ul>   #}
{% endblock %}
