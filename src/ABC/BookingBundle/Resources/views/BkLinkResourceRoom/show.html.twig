{% extends "BookingBundle::layoutMatto.html.twig" %}
{% block title%} Notification Show {%endblock%}
{% block scripthead %}
 <script src="{{ asset('Resources/js/bootstrap-dropdown.js') }}"></script>
{% endblock %}

{% block bodycontenido%}
  {#  <h1>BkNotification</h1>  #}

    <table class="table table-bordered">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Resource</th>
                <td>{{ entity.resource.name }}</td>
            </tr>
            <tr>
                <th>Room</th>
                <td>{{ entity.room.name }}</td>
            </tr>
           {# <tr>
                <th>Created date</th>
                <td>{{ entity.createdDate|date('Y-m-d H:i:s') }}</td>
            </tr>  #}
        </tbody>
    </table>
 <p> <button class="btn" data-dismiss="modal" aria-hidden="true"><i class="icon-list"></i> Back to the list</button> </p>
{#
        <ul class="record_actions">
    <li>
        <a href="{{ path('booking_mtto_bklinkresourceroom') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('booking_mtto_bklinkresourceroom_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li> 
        <form action="{{ path('booking_mtto_bklinkresourceroom_delete', { 'id': entity.id }) }}" method="post">
            <input type="hidden" name="_method" value="DELETE" />
            {{ form_widget(delete_form) }}
            <button type="submit">Delete</button>
        </form>
    </li>
</ul>#}
{% endblock %}
