{% extends "BookingBundle::layoutMatto.html.twig" %}
{% block title %} City Show {% endblock %}
{% block scripthead %}
 <script src="{{ asset('Resources/js/bootstrap-dropdown.js') }}"></script>
{% endblock %}


{% block bodycontenido %}


    <table class="table table-bordered">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ entity.name }}</td>
            </tr>
           {# <tr>
                <th>Description</th>
                <td>{{ entity.description }}</td>
            </tr>   #}
            <tr>
                <th>Quantity</th>
                <td>{{ entity.quantity }}</td>
            </tr>
            <tr>
                <th>Category of resources</th>
                <td>{{ entity.category.name }}</td>
            </tr>
            {#<tr>
                <th>Time before booking</th>
                <td>{{ entity.timeBeforeBooking|date('H:i:s') }}</td>
            </tr>
            <tr>
                <th>Is active</th>
                <td>{{ entity.isActive }}</td>
            </tr>
            <tr>
                <th>Created date</th>
                <td>{{ entity.createdDate|date('Y-m-d H:i:s') }}</td>
            </tr>
            <tr>
                <th>Timetable id</th>
                <td>{{ entity.timetableId }}</td>
            </tr>   #}
        </tbody>
    </table>
  <p>
    <button class="btn" data-dismiss="modal" aria-hidden="true"><i class="icon-list"></i> Back to the list</button>
  </p>
{#
        <ul class="record_actions">
    <li>
        <a href="{{ path('bkresource') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('bkresource_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>
        <form action="{{ path('bkresource_delete', { 'id': entity.id }) }}" method="post">
            <input type="hidden" name="_method" value="DELETE" />
            {{ form_widget(delete_form) }}
            <button type="submit">Delete</button>
        </form>
    </li>
</ul> #}
{% endblock %}
