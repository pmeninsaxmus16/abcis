{#{% extends '::base.html.twig' %}#}
{% extends "BookingBundle::layoutMatto.html.twig" %}
{% block title %} Notifications Edit {% endblock %}
{% block scripthead %}
 <script src="{{ asset('Resources/js/bootstrap-dropdown.js') }}"></script>
{% endblock %}

{% block body -%}
  {#  <h1>BkNotification edit</h1> #}

    <form action="{{ path('booking_mtto_bknotification_update', { 'id': entity.id }) }}" method="post" {{ form_enctype(edit_form) }}>
        <input type="hidden" name="_method" value="PUT" />
        {{ form_widget(edit_form) }}
        <p>
            <button class="btn" data-dismiss="modal" aria-hidden="true"><i class="icon-list"></i> Back to the list</button>
            <button type="submit" class="btn btn-primary"><i class="icon-edit icon-white"></i> Edit</button>
        </p>
    </form>
    {#
        <ul class="record_actions">
    <li>
        <a href="{{ path('bknotification') }}">
            Back to the list
        </a>
    </li>
    <li>
        <form action="{{ path('bknotification_delete', { 'id': entity.id }) }}" method="post">
            <input type="hidden" name="_method" value="DELETE" />
            {{ form_widget(delete_form) }}
            <button type="submit">Delete</button>
        </form>
    </li>
</ul> #}
{% endblock %}
