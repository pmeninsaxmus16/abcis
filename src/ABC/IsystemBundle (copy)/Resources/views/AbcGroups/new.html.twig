{% extends "ABCIsystemBundle::layoutAdmin.html.twig" %}
{% block title %} New Group {% endblock %}
{% block scripthead %}
 <script src="{{ asset('Resources/js/bootstrap-dropdown.js') }}"></script>
{% endblock %}
{% block bodycontenido -%}

    <form action="{{ path('isystem_admin_mtto_groups_create') }}" method="post" {{ form_enctype(form) }}>
        <!--{{ form_widget(form) }} -->
        {{ form_label(form.idCard) }}
        {{ form_errors(form.idCard) }}
        {{ form_widget(form.idCard) }}
        <p>
        <button class="btn" data-dismiss="modal" aria-hidden="true"><i class="icon-list"></i> Back to the list</button>                
        <button type="submit"class="btn btn-primary"><i class=""></i> Save</button>
        </p>
    </form>
{#
        <ul class="record_actions">
    <li>
        <a href="{{ path('isystem_admin_mtto_groups') }}">
            Back to the list
        </a>
    </li>
</ul>
#}
{% endblock %}
