{% extends "ABCIsystemBundle::layoutAdmin.html.twig" %}
{% block title %} Addresses Types {% endblock %}
{% block scripthead %}
 <script src="{{ asset('Resources/js/bootstrap-dropdown.js') }}"></script>
{% endblock %}


{% block bodycontenido %}
{#
<h3>{{block('title')}}</h3>
#}
    <table class="table table-bordered">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ entity.name }}</td>
            </tr>
            <tr>
                <th>Created date</th>
                <td>{{ entity.createdDate|date('H:i:s d-m-Y') }}</td>
            </tr>
        </tbody>
    </table>
        <form action="{{ path('isystem_admin_mtto_addressestype_delete', { 'id': entity.id }) }}" method="post">
        <input type="hidden" name="_method" value="DELETE" />
        {{ form_widget(delete_form) }}
        {#
        <a href="{{ path('isystem_admin_mtto_addressestype_edit', { 'id': entity.id }) }}" class="btn">
        <i class="icon-edit"></i> Edit</a>
        #}
        
       <button class="btn" data-dismiss="modal" aria-hidden="true"><i class="icon-list"></i> Back to the list</button>
      
        {#
        <a href="{{ path('isystem_admin_mtto_addressestype') }}" class="btn"><i class="icon-list"></i> Back to the list</a>
        <button type="submit" class="btn">
        <i class="icon-remove"></i> Delete</button>
        #}
        </form>

{% endblock %}
