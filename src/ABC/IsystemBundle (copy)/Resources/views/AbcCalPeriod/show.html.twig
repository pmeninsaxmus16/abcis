{% extends "ABCIsystemBundle::layoutAdmin.html.twig" %}
{% block title %} Cal Period {% endblock %}
{% block scripthead %}
 <script src="{{ asset('Resources/js/bootstrap-dropdown.js') }}"></script>
{% endblock %}


{% block bodycontenido %}
{#
<h3>{{block('title')}}</h3>
#}
    <table class="table table-bordered">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>School Your</th>
                <td>{{ entity.sy.name }}</td>
            </tr>
            <tr>
                <th>Cal</th>
                <td>{{ entity.cal.name }}</td>
            </tr>
             <tr>
                <th>Section</th>
                <td>{{ entity.section.name }}</td>
            </tr>
            <tr>
                <th>Deadline</th>
                <td>{{ entity.deadline|date('d-m-Y') }}</td>
            </tr>
            <tr>
                <th>Is Active</th>
                <td>{{ entity.isCative }}</td>
            </tr>
        </tbody>
    </table>
    <p><button class="btn" data-dismiss="modal" aria-hidden="true"><i class="icon-list"></i> Back to the list</button> </p>
    {#
    <ul class="record_actions">
    <li>
        <a href="{{ path('isystem_admin_mtto_calperiod') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('isystem_admin_mtto_calperiod_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>
        <form action="{{ path('isystem_admin_mtto_calperiod_delete', { 'id': entity.id }) }}" method="post">
            <input type="hidden" name="_method" value="DELETE" />
            {{ form_widget(delete_form) }}
            <button type="submit">Delete</button>
        </form>
    </li>
    </ul>#}
{% endblock %}
