{% extends "ABCAdmissionBundle::layout.html.twig" %}

{% block body -%}
    <h1>Applicant edit</h1>

    <form action="{{ path('admission_admin_applicant_update', { 'id': entity.id }) }}" method="post" {{ form_enctype(edit_form) }}>
        <input type="hidden" name="_method" value="PUT" />
        {{ form_widget(edit_form) }}
        <p>
            <button type="submit">Edit</button>
        </p>
    </form>

        <ul class="record_actions">
    <li>
        <a href="{{ path('admission_admin_applicant') }}">
            Back to the list
        </a>
    </li>
    <li>
        <form action="{{ path('admission_admin_applicant_delete', { 'id': entity.id }) }}" method="post">
            <input type="hidden" name="_method" value="DELETE" />
            {{ form_widget(delete_form) }}
            <button type="submit">Delete</button>
        </form>
    </li>
</ul>
{% endblock %}
